from src.pipeline import screen_candidate
def run_agent(query:str)->str:
    return ("Local mode is enabled. Use the Streamlit upload screen or the deterministic "
            "screening pipeline. No OpenAI/Anthropic API is required.")
def build_agent(): return None
